import { DatePickerUtcDirective } from './date-picker-utc.directive';

describe('DatePickerUtcDirective', () => {
  it('should create an instance', () => {
    const directive = new DatePickerUtcDirective();
    expect(directive).toBeTruthy();
  });
});
